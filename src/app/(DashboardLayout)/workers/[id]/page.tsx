import { Typography } from '@mui/material';
import React from 'react';

const Page = () => {
    return (
        <div>
            <Typography>Here will worker documents </Typography>
        </div>
    );
};

export default Page;